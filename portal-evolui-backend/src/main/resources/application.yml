user:
    timezone: America/Sao_Paulo
spring:
    application:
        name: portalEvoluiWeb
    profiles:
        # The commented value for `active` can be replaced with valid Spring profiles to load.
        # Otherwise, it will be filled in by maven when building the WAR file
        # Either way, it can be overridden by `--spring.profiles.active` value passed in the commandline or `-Dspring.profiles.active` set in `JAVA_OPTS`
        active: dev
    session:
        jdbc:
            initialize-schema: always
    liquibase:
        enabled: false #colocada a inicializacao no LiquibaseConfiguration para poder criar as tabelas primeiro
        contexts: dev
    main:
        allow-circular-references: true

server:
    port: 8087
    ssl:
#        key-store: classpath:ssl/keyStore.jks
#        key-store-password: evolui123
#        key-store-type: JKS
        key-store: D:/Sistemas/portal.pfx
        key-store-password: 123456
        key-store-type: PKCS12
    servlet:
        session:
            timeout: 120s
    error:
        include-message: always
evolui:
    app:
        jwtSecret: E52gRKszBqeoOMKtjqI1leHFKKady2P6
        jwtExpirationMs: 12000000
        externalFolder: file:///D:/Sistemas/portalEvolui/files/
    base-url: https://luthier.evoluitecnologia.com.br

wss:
    port: 8011
    token-key: 6aAeJhbrm8RYy8Nu
